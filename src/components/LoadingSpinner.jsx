import React from "react";
import { SpinnerContainer } from '../styles/Styled.LoadingSpinner';

const LoadingSpinner = () => {
    return (
        <SpinnerContainer>
            <div className="loading-spinner">

            </div>
        </SpinnerContainer>
    );
}

export default LoadingSpinner;